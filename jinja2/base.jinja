<!doctype html>
<html lang="en">
<head>
    <title>{{ brand }}</title>
    <meta charset="utf-8">
    <meta name="referrer" content="always">
    <meta name="viewport" content="width=device-width, initial-scale=1, user-scalable=0">
    <meta name="author" content="Lucian Marin">
    <meta name="description" content="24/7 Quality News">
    <link rel="apple-touch-icon" type="image/png" href="/static/192.png">
    <link rel="icon" sizes="192x192" type="image/png" href="/static/192.png">
    <link rel="manifest" type="application/json" href="/static/manifest.json">
    <link rel="stylesheet" type="text/css" href="/static/style.css?v={{v}}">
</head>
<body>
    <div class="menu">
        <h1>
            {% block menu %}
                <a class="{% if view == 'index' %}selected{% endif %}" href="{{ reverse('index') }}">
                    {{ brand }}
                </a>
                <a class="{% if view == 'recent' %}selected{% endif %}" href="{{ reverse('recent') }}">
                    Recent
                </a>
            {% endblock %}
        </h1>
    </div>
    <div class="main{% if view in ['index', 'recent'] %} {{ mode }}{% endif %}">
        {% block main %}{% endblock %}
    </div>
    <div class="footer">
        <a href="{{ reverse('index') }}">{{ brand }}</a>
        <b>&frasl;</b>
        <a href="{{ reverse('about') }}">About</a>
        <b>&frasl;</b>
        {% if mode == 'compact' %}
            <a href="{{ reverse('set', args=['mode', 'details']) }}">Details</a>
        {% else %}
            <a href="{{ reverse('set', args=['mode', 'compact']) }}">Compact</a>
        {% endif %}
        <b>&frasl;</b>
        <a>{{ count }} articles</a>
    </div>
</body>
</html>
